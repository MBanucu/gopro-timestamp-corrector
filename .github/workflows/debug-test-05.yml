name: debug-test-05

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install system deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq exiftool xvfb exfat-fuse faketime
      - name: Install project deps
        run: pip install -e .
      - name: Run test_05
        env:
          PYTHONPATH: test
        run: python -m unittest test_debug_raw_btime.DebugRawBtime.test_05_raw_write_different_cluster -v 2>&1
